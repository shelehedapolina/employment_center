<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title : 'Центр зайнятості' %></title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <nav>
    <div class="logo">📊 Центр зайнятості</div>
    <% NAV_ITEMS.forEach(item => { %>
      <a href="<%= item.href %>" class="<%= isActive(typeof active !== 'undefined' ? active : '', item.key) %>"><%= item.label %></a>
    <% }) %>
  </nav>
  <div class="container">
